name: Build Application and Make Release

on:
  push:
    tags:
      - "*"

permissions: {}

jobs:
  build_release:
    name: Build Release
    uses: ./.github/workflows/build.yml
    permissions:
      contents: read
      # Required for Azure Trusted Signing
      id-token: write
      # Required for vcpkg binary cache
      packages: write
    with:
      build-type: Release
      environment: Release
    secrets: inherit

  create_release:
    needs: build_release
    permissions:
      contents: write
    runs-on: ubuntu-slim
    outputs:
      upload_url: ${{ steps.create_release.outputs.upload_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: "true"
          path: "NitindytClient-source"
      - name: Download artifacts
        uses: actions/download-artifact@v8
      - name: Grab and store version
        run: |
          tag_name=$(echo ${{ github.ref }} | grep -oE "[^/]+$")
          echo "VERSION=$tag_name" >> $GITHUB_ENV
      - name: Package artifacts properly
        run: |
          mv ${{ github.workspace }}/NitindytClient-source NitindytClient-${{ env.VERSION }}
          mv NitindytClient-Linux-Qt6-Portable*/NitindytClient-portable.tar.gz NitindytClient-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv NitindytClient-Linux-aarch64-Qt6-Portable*/NitindytClient-portable.tar.gz NitindytClient-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
          mv NitindytClient-*.AppImage/NitindytClient-*-x86_64.AppImage NitindytClient-Linux-x86_64.AppImage
          mv NitindytClient-*.AppImage.zsync/NitindytClient-*-x86_64.AppImage.zsync NitindytClient-Linux-x86_64.AppImage.zsync
          mv NitindytClient-*.AppImage/NitindytClient-*-aarch64.AppImage NitindytClient-Linux-aarch64.AppImage
          mv NitindytClient-*.AppImage.zsync/NitindytClient-*-aarch64.AppImage.zsync NitindytClient-Linux-aarch64.AppImage.zsync
          mv NitindytClient-macOS*/NitindytClient.zip NitindytClient-macOS-${{ env.VERSION }}.zip
          mv NitindytClient-macOS*/NitindytClient.dmg NitindytClient-macOS-${{ env.VERSION }}.dmg

          tar --exclude='.git' -czf NitindytClient-${{ env.VERSION }}.tar.gz NitindytClient-${{ env.VERSION }}

          for d in NitindytClient-Windows-MSVC*; do
            cd "${d}" || continue
            LEGACY="$(echo -n ${d} | grep -o Legacy || true)"
            ARM64="$(echo -n ${d} | grep -o arm64 || true)"
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="NitindytClient-Windows-MSVC"
            test -z "${LEGACY}" || NAME="${NAME}-Legacy"
            test -z "${ARM64}" || NAME="${NAME}-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv NitindytClient-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in NitindytClient-Windows-MinGW-w64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="NitindytClient-Windows-MinGW-w64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv NitindytClient-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

          for d in NitindytClient-Windows-MinGW-arm64*; do
            cd "${d}" || continue
            INST="$(echo -n ${d} | grep -o Setup || true)"
            PORT="$(echo -n ${d} | grep -o Portable || true)"
            NAME="NitindytClient-Windows-MinGW-arm64"
            test -z "${PORT}" || NAME="${NAME}-Portable"
            test -z "${INST}" || mv NitindytClient-*.exe ../${NAME}-Setup-${{ env.VERSION }}.exe
            test -n "${INST}" || zip -r -9 "../${NAME}-${{ env.VERSION }}.zip" *
            cd ..
          done

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          tag_name: ${{ github.ref }}
          name: NITINDYT CLIENT LAUNCHER ${{ env.VERSION }}
          draft: true
          prerelease: false
          fail_on_unmatched_files: true
          files: |
            NitindytClient-Linux-x86_64.AppImage
            NitindytClient-Linux-x86_64.AppImage.zsync
            NitindytClient-Linux-aarch64.AppImage
            NitindytClient-Linux-aarch64.AppImage.zsync
            NitindytClient-Linux-Qt6-Portable-${{ env.VERSION }}.tar.gz
            NitindytClient-Linux-aarch64-Qt6-Portable-${{ env.VERSION }}.tar.gz
            NitindytClient-Windows-MinGW-w64-${{ env.VERSION }}.zip
            NitindytClient-Windows-MinGW-w64-Portable-${{ env.VERSION }}.zip
            NitindytClient-Windows-MinGW-w64-Setup-${{ env.VERSION }}.exe
            NitindytClient-Windows-MinGW-arm64-${{ env.VERSION }}.zip
            NitindytClient-Windows-MinGW-arm64-Portable-${{ env.VERSION }}.zip
            NitindytClient-Windows-MinGW-arm64-Setup-${{ env.VERSION }}.exe
            NitindytClient-Windows-MSVC-arm64-${{ env.VERSION }}.zip
            NitindytClient-Windows-MSVC-arm64-Portable-${{ env.VERSION }}.zip
            NitindytClient-Windows-MSVC-arm64-Setup-${{ env.VERSION }}.exe
            NitindytClient-Windows-MSVC-${{ env.VERSION }}.zip
            NitindytClient-Windows-MSVC-Portable-${{ env.VERSION }}.zip
            NitindytClient-Windows-MSVC-Setup-${{ env.VERSION }}.exe
            NitindytClient-macOS-${{ env.VERSION }}.zip
            NitindytClient-macOS-${{ env.VERSION }}.dmg
            NitindytClient-${{ env.VERSION }}.tar.gz
